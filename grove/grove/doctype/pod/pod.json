{
 "actions": [],
 "allow_rename": 1,
 "autoname": "prompt",
 "creation": "2026-07-22 00:00:00",
 "doctype": "DocType",
 "engine": "InnoDB",
 "field_order": [
  "tab_pod",
  "cloud_provider",
  "model",
  "column_break_head",
  "status",
  "pod_id",
  "section_image",
  "engine_image",
  "image_name",
  "template_id",
  "startup_command",
  "section_compute",
  "gpu_type_id",
  "gpu_count",
  "column_break_compute",
  "volume_in_gb",
  "container_disk_gb",
  "volume_mount_path",
  "section_env",
  "env",
  "section_net",
  "public_ip",
  "ssh_user",
  "ssh_port",
  "column_break_ports",
  "ports",
  "tab_vllm",
  "section_serve",
  "serve_port",
  "api_key",
  "column_break_serve",
  "dtype",
  "gpu_memory_utilization",
  "attention_backend",
  "max_model_len",
  "section_extra",
  "aliases",
  "extra_serve_args",
  "engine_url",
  "serve_command"
 ],
 "fields": [
  {"fieldname": "tab_pod", "fieldtype": "Tab Break", "label": "Pod"},
  {"fieldname": "cloud_provider", "fieldtype": "Link", "label": "Cloud Provider", "options": "Cloud Provider", "reqd": 1, "description": "Provider to spawn on (e.g. RunPod)."},
  {"fieldname": "model", "fieldtype": "Link", "label": "Model", "options": "Model", "in_list_view": 1, "description": "Set → this is a standalone vLLM-serving pod. The Model's intrinsic config (repo, quantization, parsers, …) is read live from the Model at build time; only per-pod tuning is set in the vLLM Serving tab."},
  {"fieldname": "column_break_head", "fieldtype": "Column Break"},
  {"fieldname": "status", "fieldtype": "Select", "label": "Status", "options": "Pending\nProvisioning\nLoading\nRunning\nStopped\nTerminated", "default": "Pending", "in_list_view": 1},
  {"fieldname": "pod_id", "fieldtype": "Data", "label": "Pod ID", "read_only": 1, "in_list_view": 1, "description": "Provider's pod id. Blank until spawned; cleared on terminate. A restart respawns → new id."},

  {"fieldname": "section_image", "fieldtype": "Section Break", "label": "Image / Startup"},
  {"fieldname": "engine_image", "fieldtype": "Link", "label": "Engine Image", "options": "Engine Image", "description": "Image to spawn, with its registry and pull credentials. Wins over the manual Image below. Ignored if a Template is set."},
  {"fieldname": "image_name", "fieldtype": "Data", "label": "Image (manual)", "description": "Escape hatch for a one-off image ref, e.g. 'vllm/vllm-openai:latest' — full path including registry host, public pulls only (no credentials). Used only when Engine Image is blank; ignored if a Template is set."},
  {"fieldname": "template_id", "fieldtype": "Data", "label": "Template ID", "description": "Provider template id. If set, the template supplies the image (Image field ignored)."},
  {"fieldname": "startup_command", "fieldtype": "Small Text", "label": "Startup Command", "depends_on": "eval:!doc.model", "description": "Overrides the image's default start command (dockerStartCmd). Used only when no Model is set (base-image pods). With a Model, the derived Serve Command (vLLM Serving tab) is used instead."},

  {"fieldname": "section_compute", "fieldtype": "Section Break", "label": "Compute"},
  {"fieldname": "gpu_type_id", "fieldtype": "Data", "label": "GPU Type ID", "description": "Provider's GPU id (e.g. 'NVIDIA L40S'). Use the Cloud Provider 'Fetch GPU Types' button for valid ids."},
  {"fieldname": "gpu_count", "fieldtype": "Int", "label": "GPU Count", "default": "1", "description": "Number of GPUs (gpuCount = tensor-parallel size). All the same type (homogeneous pod)."},
  {"fieldname": "column_break_compute", "fieldtype": "Column Break"},
  {"fieldname": "volume_in_gb", "fieldtype": "Int", "label": "Volume (GB)", "default": "50", "description": "Persistent volume disk, mounted at the mount path below. Survives pod restart."},
  {"fieldname": "container_disk_gb", "fieldtype": "Int", "label": "Container Disk (GB)", "default": "20", "description": "Ephemeral container root disk. Wiped on restart."},
  {"fieldname": "volume_mount_path", "fieldtype": "Data", "label": "Volume Mount Path", "default": "/data", "description": "Where the persistent volume mounts. HF_HOME defaults under here so weights survive restart."},

  {"fieldname": "section_env", "fieldtype": "Section Break", "label": "Environment"},
  {"fieldname": "env", "fieldtype": "Table", "label": "Env Vars", "options": "Pod Env", "description": "Extra env injected into the container. Merged over provisioner defaults (PUBLIC_KEY, HF_HOME, VLLM_API_KEY) — user rows win on conflict."},

  {"fieldname": "section_net", "fieldtype": "Section Break", "label": "Networking / SSH"},
  {"fieldname": "public_ip", "fieldtype": "Data", "label": "Public IP", "read_only": 1, "description": "Filled after spawn; shared by all exposed ports. Can move on pod restart (Sync re-reads)."},
  {"fieldname": "ssh_user", "fieldtype": "Data", "label": "SSH User", "default": "root"},
  {"fieldname": "ssh_port", "fieldtype": "Int", "label": "SSH Port", "read_only": 1, "description": "External port mapped to internal 22. Filled after spawn."},
  {"fieldname": "column_break_ports", "fieldtype": "Column Break"},
  {"fieldname": "ports", "fieldtype": "Table", "label": "Ports", "options": "Pod Port", "description": "TCP ports to open at spawn (the provider can't hot-add). Needs 22 (SSH) + the serve port + any extra engine ports. Defaults seeded on create."},

  {"fieldname": "tab_vllm", "fieldtype": "Tab Break", "label": "Serving", "depends_on": "eval:doc.model"},
  {"fieldname": "section_serve", "fieldtype": "Section Break", "label": "Serve", "description": "Per-pod serve knobs (not model-intrinsic)."},
  {"fieldname": "serve_port", "fieldtype": "Int", "label": "Serve Port", "default": "8080", "description": "Internal port vLLM listens on — must be in the Ports table (opened at spawn). Reached externally via its mapped port."},
  {"fieldname": "api_key", "fieldtype": "Password", "label": "API Key", "description": "vLLM --api-key, injected as VLLM_API_KEY env (kept out of the visible command). Auto-generated if blank."},
  {"fieldname": "column_break_serve", "fieldtype": "Column Break"},
  {"fieldname": "dtype", "fieldtype": "Select", "label": "dtype", "options": "auto\nbfloat16\nfloat16\nfloat32", "default": "auto"},
  {"fieldname": "gpu_memory_utilization", "fieldtype": "Float", "label": "GPU Memory Utilization", "default": "0.9", "precision": "2"},
  {"fieldname": "max_model_len", "fieldtype": "Int", "label": "Max Model Len", "description": "→ --max-model-len. Per-box cap for the KV cache; ≤ the model's native context. Blank = 8192."},

  {"fieldname": "section_extra", "fieldtype": "Section Break", "label": "Extra"},
  {"fieldname": "aliases", "fieldtype": "Small Text", "label": "Aliases", "description": "Extra --served-model-name values (comma/space separated); old client model-names keep working."},
  {"fieldname": "extra_serve_args", "fieldtype": "Small Text", "label": "Extra Serve Args", "description": "Freeform extra `vllm serve` flags appended verbatim, e.g. --kv-cache-dtype fp8 --max-num-seqs 64."},
  {"fieldname": "engine_url", "fieldtype": "Data", "label": "Engine URL", "read_only": 1, "description": "Where the gateway forwards: http://<public ip>:<external serve port>. Derived on sync; blank when not Running. Registered as a route for this Model in the gateway."},
  {"fieldname": "serve_command", "fieldtype": "Code", "label": "Serve Command", "read_only": 1, "options": "Shell", "description": "Derived from the fields above → the pod's dockerStartCmd. Read-only; the api-key rides VLLM_API_KEY env, not shown here."}
 ],
 "links": [],
 "modified": "2026-07-22 00:00:00",
 "modified_by": "Administrator",
 "module": "Grove",
 "name": "Pod",
 "owner": "Administrator",
 "permissions": [
  {"create": 1, "delete": 1, "email": 1, "print": 1, "read": 1, "report": 1, "role": "System Manager", "share": 1, "write": 1}
 ],
 "sort_field": "modified",
 "sort_order": "DESC",
 "states": []
}
