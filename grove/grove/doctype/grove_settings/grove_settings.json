{
 "actions": [],
 "creation": "2026-07-01 00:00:00",
 "doctype": "DocType",
 "engine": "InnoDB",
 "field_order": [
  "gateway_tab",
  "section_gateway",
  "pathway_release",
  "pathway_repo",
  "synthetic_session_ttl",
  "column_break_tls",
  "dns_provider",
  "acme_email",
  "weights_bucket_tab",
  "section_weights_bucket",
  "weights_bucket",
  "weights_s3_region",
  "column_break_weights",
  "weights_s3_access_key_id",
  "weights_s3_secret_access_key",
  "weights_s3_write_access_key_id",
  "weights_s3_write_secret_access_key",
  "monitoring_tab",
  "section_monitoring",
  "metrics_remote_write_url",
  "monitoring_extra_labels",
  "column_break_monitoring",
  "sd_token",
  "scrape_password",
  "scrape_password_hash",
  "pod_tab",
  "pod_geography"
 ],
 "fields": [
  {
   "fieldname": "section_gateway",
   "fieldtype": "Section Break",
   "label": "Gateway"
  },
  {
   "description": "The geography every Pod serves inside: only its gateways route to Pods. Blank routes no Pod anywhere.",
   "fieldname": "pod_geography",
   "fieldtype": "Link",
   "label": "Pod Geography",
   "options": "Geography"
  },
  {
   "default": "v0.0.1",
   "fieldname": "pathway_release",
   "fieldtype": "Data",
   "label": "Pathway Release"
  },
  {
   "description": "GitHub repo the release is downloaded from, as owner/name \u2014 a fork of pathway points this at itself. Its releases must carry the same assets (pathway-linux-amd64, pathway-linux-arm64, sha256sums.txt) and be public. Required: Deploy Agent and Setup refuse a blank one.",
   "fieldname": "pathway_repo",
   "fieldtype": "Data",
   "label": "Pathway Repo"
  },
  {
   "default": "0",
   "description": "How long a client that names no session of its own stays pinned to one engine. 0 balances every such request across the engines serving a model, which is the point of running two. A duration (30m, 2m) trades that for a warm prefix cache per API key \u2014 30m is what every gateway did before this field existed. A client that sends X-Grove-Session or a `user` field is always pinned and is not affected either way. Reaches a box on Deploy Agent.",
   "fieldname": "synthetic_session_ttl",
   "fieldtype": "Data",
   "label": "Synthetic Session TTL"
  },
  {
   "fieldname": "column_break_tls",
   "fieldtype": "Column Break"
  },
  {
   "description": "Whose credentials own every Geography's zone: they answer each DNS-01 challenge and write each box's records. AWS only \u2014 every zone must be a public Route53 hosted zone in that account.",
   "fieldname": "dns_provider",
   "fieldtype": "Link",
   "label": "DNS Provider",
   "link_filters": "[[\"Cloud Provider\",\"provider_type\",\"=\",\"aws\"]]",
   "options": "Cloud Provider"
  },
  {
   "description": "Where Let's Encrypt sends expiry warnings. Blank registers without one, so a stalled renewal goes unannounced.",
   "fieldname": "acme_email",
   "fieldtype": "Data",
   "label": "ACME Email"
  },
  {
   "fieldname": "section_monitoring",
   "fieldtype": "Section Break",
   "label": "Monitoring"
  },
  {
   "description": "Where a Monitoring Agent pushes when its Region names no endpoint of its own, e.g. http://13.206.253.38:8000/v1/write. Prefer https \u2014 the agent's token crosses the network on every push. The token itself lives on each agent.",
   "fieldname": "metrics_remote_write_url",
   "fieldtype": "Data",
   "label": "Metrics Remote Write URL"
  },
  {
   "fieldname": "column_break_monitoring",
   "fieldtype": "Column Break"
  },
  {
   "description": "16+ characters of A-Z a-z 0-9 - _ . ~ \u2014 e.g. `openssl rand -hex 32`. Every Monitoring Agent sends it to fetch its target list, and it is the only thing guarding that list; unset, the endpoint refuses everyone. It travels in the query string, so it appears in this site's access log \u2014 rotate it here and hit Install on each agent.",
   "fieldname": "sd_token",
   "fieldtype": "Password",
   "label": "Service Discovery Token"
  },
  {
   "description": "Guards /metrics/node and /metrics/gpu on every box \u2014 e.g. `openssl rand -hex 32`. Boxes get its hash, the agent gets the password. Unset, a box writes no htpasswd at all and nginx refuses the endpoint. Rotating it means re-running each box's play (Setup, Update Engine Config, Deploy Latest Agent) and Install on each agent.",
   "fieldname": "scrape_password",
   "fieldtype": "Password",
   "label": "Metrics Scrape Password"
  },
  {
   "description": "Derived from the password above and written to each box's htpasswd file. Recomputed only when the password changes, so re-running a play does not rewrite the file. Hidden: it is a verifier the boxes check against, with nothing an operator sets or reads.",
   "fieldname": "scrape_password_hash",
   "fieldtype": "Data",
   "hidden": 1,
   "label": "Metrics Scrape Password Hash",
   "read_only": 1
  },
  {
   "description": "JSON stamped on every series pushed from every box, e.g. {\"env\": \"prod\", \"tenant\": \"grove\"}.",
   "fieldname": "monitoring_extra_labels",
   "fieldtype": "Small Text",
   "label": "Extra Labels"
  },
  {
   "fieldname": "gateway_tab",
   "fieldtype": "Tab Break",
   "label": "Gateway"
  },
  {
   "fieldname": "weights_bucket_tab",
   "fieldtype": "Tab Break",
   "label": "Object Storage"
  },
  {
   "fieldname": "section_weights_bucket",
   "fieldtype": "Section Break",
   "label": "Weights Bucket"
  },
  {
   "description": "s3://bucket, no trailing slash. Mirrored weights live under models/, the compile-cache pre-warm under compile-cache/. Blank turns both off.",
   "fieldname": "weights_bucket",
   "fieldtype": "Data",
   "label": "Weights Bucket"
  },
  {
   "fieldname": "weights_s3_region",
   "fieldtype": "Data",
   "label": "Region"
  },
  {
   "fieldname": "column_break_weights",
   "fieldtype": "Column Break"
  },
  {
   "description": "Engine credentials, handed to every streaming engine: GetObject on models/*, Get+PutObject on compile-cache/*. Never the write pair below.",
   "fieldname": "weights_s3_access_key_id",
   "fieldtype": "Data",
   "label": "Engine Access Key ID"
  },
  {
   "fieldname": "weights_s3_secret_access_key",
   "fieldtype": "Password",
   "label": "Engine Secret Access Key"
  },
  {
   "description": "Control-plane-only credentials: PutObject on models/* (Mirror Weights To S3) + List/Get/DeleteObject on compile-cache/* (the Compile Cache registry's sync and delete). Never handed to a box.",
   "fieldname": "weights_s3_write_access_key_id",
   "fieldtype": "Data",
   "label": "Mirror Access Key ID"
  },
  {
   "fieldname": "weights_s3_write_secret_access_key",
   "fieldtype": "Password",
   "label": "Mirror Secret Access Key"
  },
  {
   "fieldname": "monitoring_tab",
   "fieldtype": "Tab Break",
   "label": "Monitoring"
  },
  {
   "fieldname": "pod_tab",
   "fieldtype": "Tab Break",
   "label": "Pod"
  }
 ],
 "issingle": 1,
 "links": [],
 "modified": "2026-09-17 10:52:41.546936",
 "modified_by": "Administrator",
 "module": "Grove",
 "name": "Grove Settings",
 "owner": "Administrator",
 "permissions": [
  {
   "create": 1,
   "delete": 1,
   "email": 1,
   "print": 1,
   "read": 1,
   "role": "System Manager",
   "share": 1,
   "write": 1
  }
 ],
 "row_format": "Dynamic",
 "sort_field": "modified",
 "sort_order": "DESC",
 "states": []
}
