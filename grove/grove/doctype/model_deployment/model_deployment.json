{
 "actions": [],
 "allow_rename": 1,
 "autoname": "format:MD-{#####}",
 "creation": "2026-07-01 00:00:00",
 "doctype": "DocType",
 "engine": "InnoDB",
 "field_order": [
  "model",
  "inference_server",
  "region",
  "column_break_engine",
  "engine_port",
  "engine_version",
  "engine_url",
  "internal_api_key",
  "status",
  "section_tuning",
  "dtype",
  "gpu_memory_utilization",
  "column_break_tuning",
  "attention_backend",
  "section_gpus",
  "gpus",
  "tensor_parallel_size"
 ],
 "fields": [
  {"fieldname": "model", "fieldtype": "Link", "label": "Model", "options": "Model", "reqd": 1, "in_list_view": 1},
  {"fieldname": "inference_server", "fieldtype": "Link", "label": "Inference Server", "options": "Inference Server", "reqd": 1},
  {"fieldname": "region", "fieldtype": "Link", "label": "Region", "options": "Region", "fetch_from": "inference_server.region", "read_only": 1},
  {"fieldname": "column_break_engine", "fieldtype": "Column Break"},
  {"fieldname": "engine_port", "fieldtype": "Int", "label": "Engine Port", "read_only": 1, "description": "Auto-allocated (read-only): lowest free port from 8080 up, scoped to this Inference Server (→ --port / vllm_port). Multi-tenant box → one port per deployment; freed on teardown and reused."},
  {"fieldname": "engine_version", "fieldtype": "Data", "label": "Engine Version", "set_only_once": 1, "description": "vLLM version to install for this deployment, e.g. 0.24.0. Empty = latest. → vllm_version (pip vllm==<ver>). Each deployment gets its OWN venv (<slug>_venv), so a box can run mixed versions side by side. Set-only-once: frozen after first save — to change version, make a new deployment."},
  {"fieldname": "engine_url", "fieldtype": "Data", "label": "Engine URL", "read_only": 1, "description": "Auto-derived (read-only): http://<machine IP>:<engine_port> — this deployment's own vLLM instance. The gateway forwards straight here (no LiteLLM); vLLM serves /v1/messages natively."},
  {"fieldname": "internal_api_key", "fieldtype": "Password", "label": "Internal API Key"},
  {"fieldname": "status", "fieldtype": "Select", "label": "Status", "options": "Draft\nProvisioning\nActive\nInactive\nTerminated\nBroken", "default": "Draft", "in_list_view": 1},
  {"fieldname": "section_tuning", "fieldtype": "Section Break", "label": "Hardware Tuning", "description": "Per-box serving knobs — depend on THIS deployment's GPU, not the portable Model profile."},
  {"fieldname": "dtype", "fieldtype": "Select", "label": "Dtype", "options": "auto\nfloat16\nbfloat16", "default": "auto"},
  {"fieldname": "gpu_memory_utilization", "fieldtype": "Float", "label": "GPU Memory Utilization", "default": "0.9", "description": "→ --gpu-memory-utilization (0-1). Depends on the GPU's VRAM headroom."},
  {"fieldname": "column_break_tuning", "fieldtype": "Column Break"},
  {"fieldname": "attention_backend", "fieldtype": "Select", "label": "Attention Backend", "options": "auto\nFLASH_ATTN\nXFORMERS\nFLASHINFER", "default": "auto"},
  {"fieldname": "section_gpus", "fieldtype": "Section Break", "label": "GPUs", "description": "Which GPUs on this box the engine runs on. Add one row per GPU (CUDA index from the Machine's GPU table). N rows → --tensor-parallel-size N + CUDA_VISIBLE_DEVICES pinned to those indices. Marked Allocated on Deploy, freed on Tear Down."},
  {"fieldname": "gpus", "fieldtype": "Table", "label": "GPUs", "options": "Model Deployment GPU"},
  {"fieldname": "tensor_parallel_size", "fieldtype": "Int", "label": "Tensor Parallel Size", "read_only": 1, "description": "Derived = number of GPU rows. 1 = single-GPU. →  --tensor-parallel-size."}
 ],
 "links": [],
 "modified": "2026-07-01 00:04:00",
 "modified_by": "Administrator",
 "module": "Grove",
 "name": "Model Deployment",
 "owner": "Administrator",
 "permissions": [
  {"create": 1, "delete": 1, "email": 1, "print": 1, "read": 1, "report": 1, "role": "System Manager", "share": 1, "write": 1}
 ],
 "sort_field": "modified",
 "sort_order": "DESC",
 "states": []
}
