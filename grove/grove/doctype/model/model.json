{
 "actions": [],
 "allow_rename": 1,
 "creation": "2026-07-01 00:00:00",
 "doctype": "DocType",
 "engine": "InnoDB",
 "field_order": [
  "provider",
  "model_id",
  "modality",
  "column_break_bhjw",
  "published",
  "provider_is_self_hosted",
  "upstream_model_id",
  "section_launch",
  "hf_repo",
  "weights_s3_uri",
  "column_break_launch",
  "enable_prefix_caching",
  "section_shape",
  "attention_heads",
  "torch_dtype",
  "hidden_layers",
  "column_break_iewq",
  "weights_gb",
  "section_tools",
  "enable_auto_tool_choice",
  "tool_call_parser",
  "column_break_reason",
  "thinking",
  "reasoning_parser"
 ],
 "fields": [
  {
   "description": "Who serves this model. Prefixes the model id (frappe/qwen3.5-4b) \u2014 set at creation, part of the name from then on. Blank names it under the Self Hosted provider; any other provider is a vendor the model routes straight to, with no engine started for it.",
   "fieldname": "provider",
   "fieldtype": "Link",
   "in_list_view": 1,
   "in_standard_filter": 1,
   "label": "Provider",
   "options": "Model Provider"
  },
  {
   "description": "The client-facing id, without its provider prefix \u2014 what a client sends as `model`. Lowercased on save, with spaces and underscores as dashes ('Qwen3.5 Coder' \u2192 qwen3.5-coder). Names the doc, so it is set once at creation.",
   "fieldname": "model_id",
   "fieldtype": "Data",
   "in_list_view": 1,
   "label": "Model ID",
   "reqd": 1,
   "set_only_once": 1
  },
  {
   "description": "What the upstream is asked for, when that is not the id above \u2014 a vendor's dated snapshot (claude-sonnet-4-5-20250929), or the name a container image advertises for itself. Blank sends the id the caller used, which is what our own engines are started under.",
   "fieldname": "upstream_model_id",
   "fieldtype": "Data",
   "label": "Upstream Model ID"
  },
  {
   "default": "text",
   "description": "Which OpenAI surfaces this model answers on. The gateway refuses the rest itself, before dialling anything: an embedding model asked for chat is a local 404, not a round trip. embedding is a pooling model served at /v1/embeddings; text also sets --language-model-only on an engine we run.",
   "fieldname": "modality",
   "fieldtype": "Select",
   "label": "Modality",
   "options": "text\nmultimodal\nembedding\naudio"
  },
  {
   "default": "0",
   "description": "Mirrored from the provider so the form and the Model link filters know whether we host it.",
   "fetch_from": "provider.is_self_hosted",
   "fieldname": "provider_is_self_hosted",
   "fieldtype": "Check",
   "hidden": 1,
   "label": "Provider Is Self Hosted",
   "read_only": 1
  },
  {
   "fieldname": "column_break_bhjw",
   "fieldtype": "Column Break"
  },
  {
   "default": "1",
   "fieldname": "published",
   "fieldtype": "Check",
   "in_list_view": 1,
   "label": "Published"
  },
  {
   "depends_on": "eval:doc.provider_is_self_hosted",
   "description": "How we serve this model ourselves \u2014 the repo, the weights, and the vllm role vars (playbooks/inference_server). Blank = flag omitted / role default. Absent for a vendor's model: there is no engine to launch.",
   "fieldname": "section_launch",
   "fieldtype": "Section Break",
   "label": "Launch Profile"
  },
  {
   "description": "\u2192 vllm serve ; the name is the --served-model-name. A GGUF repo ships many quantizations \u2014 name one with repo:QUANT (unsloth/Qwen3-0.6B-GGUF:Q4_K_M) so only that file is pulled, and add --tokenizer &lt;base repo&gt; to Extra Serve Args on the deployment, since converting a GGUF's own tokenizer is slow and unreliable. Single-file quantizations only; a split one has to be merged first.",
   "fieldname": "hf_repo",
   "fieldtype": "Data",
   "label": "HF Repo",
   "mandatory_depends_on": "eval:doc.provider_is_self_hosted"
  },
  {
   "description": "S3 mirror of the safetensors weights. When set, engines stream them straight from S3 to the GPU (runai streamer) instead of downloading from HF \u2014 fill it with Mirror Weights To S3, or clear it to fall back to HF.",
   "fieldname": "weights_s3_uri",
   "fieldtype": "Data",
   "label": "Weights S3 URI"
  },
  {
   "fieldname": "column_break_launch",
   "fieldtype": "Column Break"
  },
  {
   "default": "1",
   "description": "\u2192 --enable-prefix-caching",
   "fieldname": "enable_prefix_caching",
   "fieldtype": "Check",
   "label": "Enable Prefix Caching"
  },
  {
   "depends_on": "eval:doc.provider_is_self_hosted",
   "description": "From the repo's config.json. Used to reject a GPU split the model can't run before it reaches the box. Blank = that check is skipped.",
   "fieldname": "section_shape",
   "fieldtype": "Section Break",
   "label": "Architecture"
  },
  {
   "description": "num_attention_heads. vLLM shards heads across the tensor-parallel group, so this must divide evenly by tensor-parallel size (e.g. 64 heads cannot run on 6 GPUs).",
   "fieldname": "attention_heads",
   "fieldtype": "Int",
   "label": "Attention Heads"
  },
  {
   "description": "torch_dtype from the repo's config.json \u2014 what vLLM serves these weights in unless a placement overrides it. bfloat16 needs a card of compute capability 8.0 or better and hard-raises below it, so a placement onto an older card is rejected here rather than minutes into a play. Blank = that check is skipped.",
   "fieldname": "torch_dtype",
   "fieldtype": "Data",
   "label": "Torch Dtype",
   "read_only": 1
  },
  {
   "description": "num_hidden_layers. Informational \u2014 vLLM splits these across the pipeline-parallel group and spreads any remainder, so the count need not divide evenly.",
   "fieldname": "hidden_layers",
   "fieldtype": "Int",
   "label": "Hidden Layers"
  },
  {
   "fieldname": "column_break_iewq",
   "fieldtype": "Column Break"
  },
  {
   "description": "Size of the weights, summed per dtype from the repo's safetensors index. A placement whose GPUs can't hold this much is rejected \u2014 note it counts weights only, so the KV cache and activations need headroom on top.",
   "fieldname": "weights_gb",
   "fieldtype": "Float",
   "label": "Weights (GB)",
   "precision": "2"
  },
  {
   "depends_on": "eval:doc.provider_is_self_hosted",
   "fieldname": "section_tools",
   "fieldtype": "Section Break",
   "label": "Tools & Reasoning"
  },
  {
   "default": "1",
   "description": "\u2192 --enable-auto-tool-choice",
   "fieldname": "enable_auto_tool_choice",
   "fieldtype": "Check",
   "label": "Enable Auto Tool Choice"
  },
  {
   "description": "\u2192 --tool-call-parser. e.g. hermes (Qwen2.5), qwen3_coder (Qwen3.6/3.5).",
   "fieldname": "tool_call_parser",
   "fieldtype": "Data",
   "label": "Tool Call Parser"
  },
  {
   "fieldname": "column_break_reason",
   "fieldtype": "Column Break"
  },
  {
   "default": "0",
   "description": "If set, the reasoning parser below is applied.",
   "fieldname": "thinking",
   "fieldtype": "Check",
   "label": "Reasoning Model"
  },
  {
   "description": "\u2192 --reasoning-parser (only when Reasoning Model is checked). e.g. qwen3.",
   "fieldname": "reasoning_parser",
   "fieldtype": "Data",
   "label": "Reasoning Parser"
  }
 ],
 "links": [
  {
   "group": "Serving",
   "link_doctype": "Model Deployment",
   "link_fieldname": "model"
  },
  {
   "group": "Placements",
   "link_doctype": "Model Replica",
   "link_fieldname": "model"
  },
  {
   "group": "Placements",
   "link_doctype": "Pod",
   "link_fieldname": "model"
  }
 ],
 "modified": "2026-09-17 12:00:00.000000",
 "modified_by": "Administrator",
 "module": "Grove",
 "name": "Model",
 "owner": "Administrator",
 "permissions": [
  {
   "create": 1,
   "delete": 1,
   "email": 1,
   "print": 1,
   "read": 1,
   "report": 1,
   "role": "System Manager",
   "share": 1,
   "write": 1
  },
  {
   "read": 1,
   "role": "Grove Control"
  }
 ],
 "row_format": "Dynamic",
 "sort_field": "modified",
 "sort_order": "DESC",
 "states": []
}
